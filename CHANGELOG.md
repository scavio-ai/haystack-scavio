# Changelog

## [v0.1.0]

### Features

- Add `ScavioWebSearch` component: web search backed by the Scavio API, returning
  Haystack `Document`s and source links, with sync `run` and async `run_async`.
